spring:
  application:
    name: config
  #  profiles:
  #    active: development
  cloud:
    kubernetes:
      config:
        sources:
          - name: ${spring.application.name}
            namespace: default